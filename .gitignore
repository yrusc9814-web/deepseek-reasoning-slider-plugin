# dependencies
node_modules/

# build scratch (esbuild intermediate; committed output lives in lib/)
.client-build/
dist/

# leftover tsc emits that must never overwrite the wrapped client bundle
lib/client/styles.js
lib/client/styles.d.ts

# logs / OS / editor
*.log
npm-debug.log*
pnpm-debug.log*
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# local secrets and environment
.env
.env.*
!.env.example
*.pem
*.key
credentials*.yaml
.credentials.yaml

# local scratch
tmp/
temp/
*.tmp

# unused / generated sprite leftovers
assets/*-chroma.png
assets/chibi-runner.png
